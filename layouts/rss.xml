{{- /*
  rss.xml — FULL-CONTENT feed (specs/003 §3.7).

  Hugo's built-in RSS template emits `.Summary`, which for this corpus means a feed
  reader shows the first 70 words of a procedure and then makes you open a browser to
  read the command you came for. Full content is the point of a feed for a technical
  blog.

  `transform.XMLEscape` on `.Content` rather than a CDATA section: CDATA cannot
  contain the sequence `]]>`, and this archive is 9,046 fenced code blocks of shell
  and YAML. Escaping is the only form that cannot be broken by the content.

  ▸ SIZE. Full content × a large archive is a large feed. The reference site's 497
    posts would be several megabytes. `services.rss.limit` is honoured exactly as the
    internal template honours it, so a consumer caps it in their own config:

        [services.rss]
          limit = 20

  ▸ NO `languageCode`. It was deprecated in Hugo v0.158.0 and its replacement does not
    exist at the v0.146.0 floor (contracts §3), so either spelling breaks a supported
    version under `--panicOnWarning`. `site.Language.Lang` is stable across the range.

  Dates are guarded: `.Date.IsZero` produces no `<pubDate>` rather than a 1970
  timestamp that sorts a dateless post to the beginning of every feed reader.
*/ -}}
{{- /* Page selection, per kind. Two bugs lived in the two lines this replaces, and
       both were found by building the real 497-post archive rather than exampleSite.

  HOME used site.RegularPages, which is every regular page on the site — so /about-us/,
  /contact-us/, /privacy-policy/ and /search/ published as feed items. mainSections is
  the configurable answer to "what counts as a post" and is what the rest of the theme
  already uses (specs/003 §3.4); a feed is exactly the place it matters, because a
  subscriber gets the privacy policy delivered as news.

  TAXONOMY (/tags/, /categories/) used .RegularPages, which on a taxonomy list kind is
  EMPTY — the children of /tags/ are term pages, not articles. So /tags/index.xml and
  /categories/index.xml were valid, well-formed, entirely empty feeds. Against the
  reference archive that is 295 and 28 items going to zero, and nothing failed: an
  empty feed is not an error to any validator.
  Term pages (/tags/linux/) were always fine — .RegularPages is correct there. */ -}}
{{- $mainPages := where site.RegularPages "Section" "in" site.Params.mainSections -}}
{{- $pages := .RegularPages -}}
{{- if .IsHome -}}
  {{- $pages = $mainPages -}}
{{- else if eq .Kind "taxonomy" -}}
  {{- /* Every page carrying any term in this taxonomy, newest first. Collected from
         the terms rather than taken as "all posts", so a site whose taxonomy covers
         part of the corpus gets a feed that means what its URL says. */ -}}
  {{- $collected := slice -}}
  {{- range .Pages }}{{ range .Pages }}{{ $collected = $collected | append . }}{{ end }}{{ end -}}
  {{- /* NOT `$collected | uniq | sort "Date" "desc"`. A Go template pipeline appends
         the piped value as the LAST argument, so that spelling calls
         sort "Date" "desc" (uniq $collected) and fails with `can't sort string`. */ -}}
  {{- $pages = sort (uniq $collected) "Date" "desc" -}}
{{- end -}}
{{- $limit := site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 }}{{ $pages = $pages | first $limit }}{{ end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if .IsHome }}{{ site.Title }}{{ else }}{{ partial "utils/term-title.html" . }} on {{ site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ with .Description | default site.Params.description }}{{ . | plainify }}{{ end }}</description>
    <generator>Hugo</generator>
    <language>{{ site.Language.Lang }}</language>
    {{- with $pages }}{{ with index . 0 }}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end }}{{ end }}{{ end }}
    {{- with .OutputFormats.Get "rss" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType.Type | safeHTML }}
    {{- end }}
    {{- range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      {{- if not .Date.IsZero }}
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- end }}
      <guid isPermaLink="true">{{ .Permalink }}</guid>
      {{- range partial "utils/terms.html" (dict "page" . "taxonomy" "categories") }}
      <category>{{ partial "utils/term-title.html" . }}</category>
      {{- end }}
      <description>{{ .Content | transform.XMLEscape | safeHTML }}</description>
    </item>
    {{- end }}
  </channel>
</rss>
