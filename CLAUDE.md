# CLAUDE.md

@AGENTS.md

<!--
  Instructions for this repository live in AGENTS.md — the cross-tool convention that
  Codex, Cursor, Copilot, Jules, Aider and Zed also read. Claude Code reads CLAUDE.md,
  so this file imports it and nothing is duplicated.

  An import rather than a symlink on purpose: a symlink is Unix-only, and a Windows
  checkout without core.symlinks materialises it as a text file containing the string
  "AGENTS.md", which reads as an empty instruction set with no error. This works
  everywhere.

  Do not add instructions here. Edit AGENTS.md.
-->
